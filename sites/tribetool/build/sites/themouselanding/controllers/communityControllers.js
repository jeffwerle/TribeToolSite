angular.module('cthemouselanding.Controllers')
    .controller('cthemouselandingCommunityController', ['$scope', function($scope) {

        // This controller will be called any time the /community page is loaded

        $scope.initialized = function() {
        };

    }]);