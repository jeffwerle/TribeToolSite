angular.module('app.Services')
    /* Lightbox mocking for mobile (we'll use something else later) */
    .factory('Lightbox', ['$rootScope', function($rootScope) {
        return {
        };
    }]);